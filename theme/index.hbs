{{!< default}}

{{#is "home"}}
{{!-- ====================== HOMEPAGE — practitioner's calling card ====================== --}}
<!--
  flag{w3lc0me_t0_r00tdrift3r}

  You found the first flag. You're already thinking like a security professional.
  Viewing source to find hidden information is one of the first things any security
  researcher does — congratulations.

  Submit: hello@rootdrifter.io  ·  Subject: Challenge 0 — [your handle]
  More challenges: https://rootdrifter.io/challenges/
-->{{!-- CTF Challenge 0 (view-source flag) — homepage only; HTML comment, invisible in render --}}

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Person",
  "name": "rootdrifter",
  "alternateName": "RootDrifter",
  "description": "Security-cleared practitioner — offensive methodology, systems hardening, detection engineering.",
  "url": "{{@site.url}}",
  "sameAs": [
    "https://github.com/rootdrifter",
    "https://rootdrifter.io"
  ]
}
</script>

<header class="hero">
  <canvas id="rain" aria-hidden="true"></canvas>
  <div class="container">
    <div class="sys-label">// rootdrifter — security practitioner</div>
    <h1>Root<span class="slash">//</span>Drifter</h1>
    <div class="hero-sub">Hardened systems · offensive methodology · detection engineering</div>
    <p class="typed-line" aria-label="Security-cleared and deployable from day one, with no vetting delay.">
      <span id="typed" data-p1="{{@custom.hero_tagline_1}}" data-p2="{{@custom.hero_tagline_2}}" data-p3="{{@custom.hero_tagline_3}}"></span><span class="cursor" aria-hidden="true">&#9608;</span>
    </p>
    <noscript><p class="typed-line">Security-cleared. Deployable from day one — no vetting delay.</p></noscript>
    <div class="status-row">
      <span class="status-tag st-cleared">&#9679; Cleared</span>
      <span class="status-tag st-active">&#9679; Active</span>
      <span class="status-tag st-avail">&#9679; Available</span>
    </div>
    <div class="hero-links">
      <a class="btn" href="{{@site.url}}/portfolio/">View portfolio &#8594;</a>
      <a class="btn ghost" href="{{@site.url}}/blog/">Read the transmission log &#8595;</a>
    </div>
  </div>
</header>

<div class="container">

  {{!-- CIVILIAN ON-RAMP + MANIFESTO STRIP (declare, don't present — above the portfolio) --}}
  <p class="civilian-lede">Not technical? <a href="{{@site.url}}/start/">Start here &#8594;</a></p>
  <div class="manifesto-strip">
    <p class="manifesto-text">Security knowledge is a right, not a product. rootdrifter is building in public to prove it.</p>
  </div>

  {{!-- ABOUT --}}
  <div class="section-break"><h2>About</h2></div>
  <p>rootdrifter is the working handle of a security-cleared professional building toward cleared
  entry-level roles in the UK, Netherlands, and Germany. The clearance is held now — not pending a
  conditional offer — which is stated here as a plain fact rather than a pitch: it means deployment to
  cleared work without the months-long vetting wait most graduates carry.</p>
  <p>The approach is to build and to break with the same discipline, and to document both to a standard
  a reviewer can audit. That has produced a hardened Linux workstation and a compartmentalised mobile
  platform, a grey-box penetration test mapped to recognised benchmarks, and dissertation-level research
  into whether language models can reason causally about social engineering rather than just pattern-match
  it. Current focus: CompTIA Security+, active CTF practice, and a Wazuh SIEM home lab for hands-on
  detection engineering.</p>
  <p>Academic background: a First Class BSc in Computer Networks &amp; Cyber Security. This site
  stays pseudonymous by design — a named CV with full academic and contact detail is available on
  request.</p>

  {{!-- PORTFOLIO STRIP --}}
  <div class="section-break"><h2>Portfolio</h2></div>
  <div class="pf-strip">
    <a class="pf-item" href="{{@site.url}}/portfolio/ironveil/">
      <span class="pf-kind">ironveil · defensive</span>
      <p>Hardened Fedora workstation — LUKS2 full-disk encryption unlocked by a FIDO2 hardware key,
      remote pre-boot SSH unlock, and all DNS filtered before it leaves the host.</p>
      <span class="pf-metric">3-keyslot LUKS2 · Nitrokey 3A NFC · WireGuard egress</span>
    </a>
    <a class="pf-item" href="{{@site.url}}/portfolio/spectre/">
      <span class="pf-kind">spectre · offensive</span>
      <p>Grey-box pentest of an Apache 2.4.58 host from a CIS-L1 PostgreSQL server — PTES, findings
      mapped to CWE / CIS / ISO&nbsp;27002, SHA-256 evidence chain.</p>
      <span class="pf-metric">7 findings · primary CWE-548 · SHA-256 chain</span>
    </a>
    <a class="pf-item" href="{{@site.url}}/portfolio/mirage/">
      <span class="pf-kind">mirage · research</span>
      <p>Causal LLM evaluation over an 88,647-email phishing corpus — can frontier models reason about
      social engineering, or only pattern-match it?</p>
      <span class="pf-metric">88,647 emails · ICC 0.98 · GPT-4 94.2% DAG</span>
    </a>
  </div>
  <p><a href="{{@site.url}}/portfolio/"><strong>Full portfolio &#8594;&nbsp;&nbsp;all six projects</strong></a></p>

  {{!-- LATEST WRITING --}}
  <div class="section-break"><h2>Latest writing</h2></div>
  {{#get "posts" limit="3" include="tags" as |recent|}}
    {{#if recent}}
      <div class="post-feed">
        {{#foreach recent}}{{> post-card}}{{/foreach}}
      </div>
      <p><a href="{{@site.url}}/blog/">All posts &#8594;</a></p>
      {{!-- hard-coded teaser count — update as posts are reviewed (see content/APPROVAL_QUEUE.md) --}}
      <p class="transmission-count">More transmissions incoming &middot; Subscribe for early access</p>
    {{else}}
      <div class="info-block">
        <span class="blk-label">// transmission-log</span>
        Writing goes live here shortly — CTF writeups and repeatable security methodology, the same
        evidence-led format as the portfolio.
      </div>
    {{/if}}
  {{/get}}

  {{> subscribe}}

  {{!-- ACTIVE WORK --}}
  <div class="section-break"><h2>Active work</h2></div>
  <div class="ops-board">
    <div class="ops-row"><span class="ops-tag st-progress">In progress</span> CompTIA Sec+ SY0-701</div>
    <div class="ops-row"><span class="ops-tag st-progress">In progress</span> Wazuh SIEM home lab — detection engineering</div>
    <div class="ops-row"><span class="ops-tag st-active">Active</span> CTF practice — TryHackMe / HackTheBox</div>
    <div class="ops-row"><span class="ops-tag st-active">Live</span> rootdrifter.io — content platform, publishing progressively</div>
    <div class="ops-row"><span class="ops-tag st-avail">Available</span> Cleared entry-level roles — UK / NL / DE</div>
  </div>

  {{!-- CONTACT --}}
  <div class="section-break"><h2>Contact</h2></div>
  <div class="info-block">
    <span class="blk-label">// available for cleared work</span>
    <p><a href="mailto:hello@rootdrifter.io">hello@rootdrifter.io</a>
    &nbsp;&middot;&nbsp; <a href="https://github.com/rootdrifter" rel="noopener">github.com/rootdrifter</a></p>
    <p>Open to cleared security roles in the UK, Netherlands, and Germany. CV available on request.</p>
  </div>

</div>

<script>
(function () {
  'use strict';
  var reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  var el = document.getElementById('typed');
  if (!el) return;
  /* taglines are theme custom settings (Ghost admin → Design), surfaced via data attributes */
  var PHRASES = [
    el.getAttribute('data-p1') || 'Security-cleared. Deployable from day one.',
    el.getAttribute('data-p2') || 'Cleared professional. No vetting delay.',
    el.getAttribute('data-p3') || 'Building toward cleared security roles.'
  ];
  if (reduce) { el.textContent = PHRASES[0]; return; }
  var p = 0, c = 0, del = false;
  (function tick() {
    var s = PHRASES[p];
    if (!del) {
      el.textContent = s.slice(0, ++c);
      if (c === s.length) { del = true; setTimeout(tick, 2000); return; }
      setTimeout(tick, 50);
    } else {
      el.textContent = s.slice(0, --c);
      if (c === 0) { del = false; p = (p + 1) % PHRASES.length; setTimeout(tick, 350); return; }
      setTimeout(tick, 30);
    }
  })();

  /* hero terminal-rain canvas — parity with the static hub, reduced-motion aware */
  var canvas = document.getElementById('rain');
  if (canvas && !reduce) {
    var ctx = canvas.getContext('2d');
    var CHARS = '0123456789ABCDEF#$%&>_/\\|', FONT = 14, drops = [], last = 0;
    function resize() {
      var hero = canvas.parentElement;
      canvas.width = hero.clientWidth; canvas.height = hero.clientHeight;
      var cols = Math.floor(canvas.width / FONT); drops = [];
      for (var i = 0; i < cols; i++) drops.push(Math.random() * canvas.height / FONT);
    }
    function draw(ts) {
      requestAnimationFrame(draw);
      if (document.hidden || ts - last < 66) return;  /* ~15fps drift */
      last = ts;
      ctx.fillStyle = document.documentElement.classList.contains('light') ? 'rgba(240,244,248,0.20)' : 'rgba(9,13,18,0.18)'; ctx.fillRect(0, 0, canvas.width, canvas.height);
      ctx.fillStyle = '#00e5ff'; ctx.font = FONT + "px 'Share Tech Mono', monospace";
      for (var i = 0; i < drops.length; i++) {
        ctx.fillText(CHARS.charAt(Math.floor(Math.random() * CHARS.length)), i * FONT, drops[i] * FONT);
        if (drops[i] * FONT > canvas.height && Math.random() > 0.975) drops[i] = 0;
        drops[i] += 0.5;
      }
    }
    resize(); window.addEventListener('resize', resize); requestAnimationFrame(draw);
  }
})();
</script>

{{else}}
{{!-- ====================== /blog/ COLLECTION LISTING ====================== --}}
<div class="container">
  <div class="section-break"><h2>Writing</h2></div>
  {{#if posts}}
    <div class="post-feed">
      {{#foreach posts}}{{> post-card}}{{/foreach}}
    </div>
    {{pagination}}
  {{else}}
    <div class="info-block">
      <span class="blk-label">// transmission-log</span>
      No posts published yet. CTF writeups and security methodology are on the way.
    </div>
  {{/if}}
</div>
{{/is}}
